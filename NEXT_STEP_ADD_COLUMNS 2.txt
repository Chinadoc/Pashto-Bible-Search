🎯 NEXT STEP: Add Inflection Columns to Supabase

⏱️ TIMING: This takes ~30 seconds

📋 WHAT TO DO:

1. Go to Supabase Dashboard: https://supabase.com/dashboard/project/nkombdutnjvaasxrbmdn/sql/new

2. Copy and paste this SQL:

```sql
ALTER TABLE word_occurrence_index
ADD COLUMN IF NOT EXISTS is_inflected BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS base_word TEXT;

CREATE INDEX IF NOT EXISTS idx_word_occurrence_is_inflected ON word_occurrence_index(is_inflected);
```

3. Click "Run"

4. Wait for success message

5. Then reply "done" and I'll run the expansion script

✨ This will enable us to track which words are inflected forms, so we can:
- Add all conjugations and inflections (629K+ forms)
- Make ALL searches fast (<100ms)
- Remove JSON fallback entirely
